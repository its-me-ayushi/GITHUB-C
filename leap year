#include<stdio.h>
int main()
{
    int yr;
    printf("\enter any year:");
    scanf("^%d",&yr);
    if(yr%400 == 0)||(yr%100 != 0 && yr%4 == 0))
    printf("\n%d is not a leap year:",yr);
    getch();
    return 0;
 }   
