#include <iostream>

using namespace std;


    void result(int b[5])
    {
      cout<<"enter five subject"<<endl;
      for(int i=0;i<5;i++)
    {
      cout<<"subject"<<i+1<<" ";
    }
    b[3]=1000;
    }
    int main()
    {
        int m[5],total;
        float per;
        cout<<"enter five subject"<<endl;
        for(int i=0;i<5;i++)
        {
            cout<<"subject"<<i+1<<" ";
            cin>>m[i];
        }
        result(m);
        
        cout<<"enter tthe marks:"<<endl;
        for(int i=0;i<5;i++)
        {
            cout<<"subject"<<i+1<<" "<<m[i]<<endl;
            
        }
        
    }
